# Release manifest. This file contains no secrets and is committed to Git.
# For a new major/minor, set RELEASE_VERSION to X.Y.0; `make release` publishes X.Y.1.
RELEASE_VERSION=0.1.0
RELEASE_COMMIT=
REGISTRY=registry.production.local
IMAGE=ml-inference-service
BLUE_PORT=18001
GREEN_PORT=18002
